---
id: intro
title: "Primeros Pasos"
sidebar_position: 1
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# 🚀 Primeros Pasos con MATICERTS

Bienvenido a **MATICERTS**, la plataforma moderna para emisión y gestión de certificados digitales. Esta documentación te guiará paso a paso para integrar nuestra API REST en tu aplicación.

:::info ¿Qué es MATICERTS?
MATICERTS es una solución empresarial que permite emitir, validar y gestionar certificados digitales con seguridad OAuth 2.0. Perfecta para ERPs, CRMs, portales web y sistemas automatizados.
:::

---

## 🌍 Elige tu Entorno

Contamos con dos entornos separados para que desarrolles sin riesgos:

**Sandbox (Pruebas)** → Perfecto para aprender y probar  
**Producción** → Para certificados con validez legal

Parametriza la base URL en tu aplicación (`{{URL}}`) apuntando al entorno deseado:

<div className="row margin-bottom--lg">
  <div className="col col--6">
    <div className="card shadow--md">
      <div className="card__header">
        <h3>🧪 Sandbox (Pruebas)</h3>
      </div>
      <div className="card__body">
        <p>Entorno efímero y gratuito para el ciclo de desarrollo.</p>
        <div className="alert alert--secondary">
          <code>https://sandbox-api.maticerts.com</code>
        </div>
      </div>
    </div>
  </div>
  <div className="col col--6">
    <div className="card shadow--md">
      <div className="card__header">
        <h3>🚀 Producción</h3>
      </div>
      <div className="card__body">
        <p>Infraestructura real. Emite certificados con validez legal.</p>
        <div className="alert alert--success">
          <code>https://api.maticerts.com</code>
        </div>
      </div>
    </div>
  </div>
</div>

:::warning 🔒 Buena Práctica de Seguridad
**Nunca hardcodees las URLs en el código.** Usa variables de entorno (`.env`) para cambiar entre Sandbox y Producción sin recompilar tu aplicación.
:::

---

## 🔐 Autenticación: Elige tu Método

Todas las peticiones a MATICERTS deben incluir un **Bearer Token** en el encabezado `Authorization`. Ofrecemos dos métodos según tu caso de uso:

### Métodos Soportados

<Tabs groupId="auth-methods">
  <TabItem value="m2m" label="🤖 Backend (M2M) - Recomendado" default>
    <p>
      <strong>Ideal para:</strong> ERPs, CRMs, Cronjobs, integraciones automatizadas.
    </p>
    <p>
      Usa un <strong>Personal Access Token (PAT)</strong> generado en el panel de administración. Estos tokens:
    </p>
    <ul>
      <li>✅ No expiran automáticamente</li>
      <li>✅ Máximo rendimiento</li>
      <li>✅ Fácil de gestionar</li>
    </ul>
  </TabItem>
  <TabItem value="frontend" label="👤 Aplicaciones Cliente">
    <p>
      <strong>Ideal para:</strong> Portales web, aplicaciones móviles, interfaces de usuario.
    </p>
    <p>
      Obtén un token temporal autenticando al usuario final:
    </p>
    
```bash
curl -X POST "{{URL}}/api/v1/auth/login" \
     -H "Content-Type: application/json" \
     -d '{
           "email": "usuario@empresa.com",
           "password": "tu-contraseña-segura"
         }'
```
  </TabItem>
</Tabs>

---

## 🛠️ Tu Primera Petición: Health Check

Verifica que tu conexión funciona correctamente. Reemplaza `<TU_ACCESS_TOKEN>` con tu token real:

<Tabs groupId="programming-languages">
  <TabItem value="curl" label="cURL" default>

```bash
curl -X GET "{{URL}}/api/v1/sistema/health" \
     -H "Authorization: Bearer <TU_ACCESS_TOKEN>" \
     -H "Accept: application/json"
```

  </TabItem>
  <TabItem value="javascript" label="Node.js / Fetch">

```javascript
const response = await fetch('{{URL}}/api/v1/sistema/health', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer <TU_ACCESS_TOKEN>',
    'Accept': 'application/json'
  }
});
const data = await response.json();
console.log(data);
```

  </TabItem>
  <TabItem value="python" label="Python (Requests)">

```python
import requests

url = "{{URL}}/api/v1/sistema/health"
headers = {
    "Authorization": "Bearer <TU_ACCESS_TOKEN>",
    "Accept": "application/json"
}

response = requests.get(url, headers=headers)
print(response.json())
```

  </TabItem>
</Tabs>

<br/>

---

## 📚 Próximos Pasos

¡Excelente! Ya tienes tu primer endpoint funcionando. Ahora:

1. **Explora el Catálogo de API** en el panel lateral para descubrir todos los endpoints disponibles
2. **Lee la guía paso a paso** para implementar tu primer flujo de emisión de certificados
3. **Consulta casos de uso** para ver ejemplos reales de integración
4. **Revisa troubleshooting** si encuentras algún problema

:::success 🎉 ¿Necesitas ayuda?
Si tienes dudas, consulta nuestra sección de **Preguntas Frecuentes** o contacta al equipo de soporte.
:::
